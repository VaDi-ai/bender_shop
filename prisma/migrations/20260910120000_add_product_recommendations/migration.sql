-- Рекомендуем: ручные замены слотов в карточке товара.
--
-- Аддитивно и инертно: ни UNIQUE (урок #107 — unique на старте валит db push и
-- гасит контейнер), ни NOT NULL без дефолта, ни бэкфилла. Существующие строки
-- получают пустой массив, а пустой массив читается витриной как «считай всё
-- алгоритмом» — то есть поведение до миграции и после неё совпадает.
--
-- Внешнего ключа тут намеренно нет: массив, а не связь. Удалённый товар
-- оставляет висячий id, и это чинится на чтении (слот уходит на авто), а не
-- каскадом, который переписывал бы чужие карточки молча.

-- AlterTable
ALTER TABLE "Product" ADD COLUMN     "recommendedIds" INTEGER[] DEFAULT ARRAY[]::INTEGER[];
